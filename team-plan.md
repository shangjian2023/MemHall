# 麟阁 顶层设计与五人分工协作方案

> 项目全称：**麟阁——面向 openKylin 生态的智能体记忆能力评测基准**（MemHall，仓库名/CLI/包名 `memhall`）
> 名字取自麒麟阁：汉代评定功臣、画像记名的殿堂——给记忆能力优秀的智能体立榜。

> 赛题：面向 openKylin 操作系统的智能体长期记忆自动化评测 Benchmark 设计（开源 AI 工具赛道）
> 本文档 = 项目蓝图 + 排期 + 协作规约。技术设计详见 **design.md**（总体技术设计 v1.0，交付 a 底稿）。
> **排期基线（2026-09-23 重排）**：实际赛期 4 周——报名截止 **10.11**，总决赛 **10.24（上海）**；
> 作品提交截止时间待官网确认（A 报名后第一时间回填本行，若早于 10.18 则 W4 收口相应提前）。

---

## 一、总体架构

### 1.1 一句话定位

**麟阁**：一套跑在 openKylin 上的智能体长期记忆评测基准——用「三阶段会话剧本」驱动任意智能体，把对话日志、记忆快照、行动轨迹、文件产物统一为**证据**，自动评分并输出**六维能力雷达图**。

### 1.2 系统分层

```
┌────────────────────────────────────────────────┐
│  CLI 前端  memhall run/compare/report          │  一键运行、批量对比（交付 d）
├────────────────────────────────────────────────┤
│  编排层（Runner）                                │
│   三阶段剧本执行 → 适配器调用 → 证据采集落盘      │
├──────────────────────┬─────────────────────────┤
│  适配层（Adapters）   │  数据层（Cases）          │
│  AgentAdapter 统一接口│  用例 schema + 生成器    │
│  KylinBot/OpenClaw/… │  + 种子用例库            │
├──────────────────────┴─────────────────────────┤
│  评分层（Scoring & Report）                      │
│  规则验证器 + LLM-judge → 五态判定               │
│  → 多维指标 + 雷达图 + 可解释报告                │
└────────────────────────────────────────────────┘
```

### 1.3 四个核心设计决策（直接对准评分表）

**决策 1：剧本驱动、证据本位（对应数据设计 25%）**

每个测试用例 = 一个剧本，分三阶段：

- **注入段（inject）**：多轮对话注入记忆目标，混装四类样本——正样本（该记）、负样本（不该记）、待更新样本（先说 A 再改 B）、相似干扰对（~/proj/api vs ~/proj/api-v2）
- **混淆段（confound）**：插入无关话题若干轮 + `end_session` 重置会话/重启进程，模拟"长期"与"时间流逝"
- **探测段（probe）**：问答探测（问已知信息）+ 行为探测（下发任务，看是否复用历史路径/模板）

每阶段自动采集四类证据：对话全量日志、智能体记忆库 dump、行动轨迹、文件系统 diff。

**决策 2：适配器统一接口（对应任务定义与通用性 15%）**

```python
class AgentAdapter:
    def reset() -> None                      # 清零记忆，保证可复现
    def send(session_id, message) -> Reply
    def dump_memory() -> MemorySnapshot      # 记忆库导出（接口没有就直读文件/SQLite）
    def dump_actions() -> list[Action]
    def end_session() -> None                # 会话隔离，模拟跨会话
```

每个智能体一个适配器，配置文件驱动（`agents/*.yaml`）。"批量对比评测" = 遍历配置目录逐个跑同一套用例。

**决策 3：混合评分器（对应自动评分 25%）**

- **L1 规则层**：确定性断言（精确匹配、正则、路径存在性、记忆库中敏感串出现/未出现、文件 diff）——零成本、零漂移，能规则化的绝不用 LLM
- **L2 语义层**：LLM-as-judge，三重稳定化——rubric 锚定 + few-shot 锚定例 + 双 judge 独立判（不一致 → 规则仲裁，仍不定 → 标记人工复核位）
- **判定输出五态**（对准评分细则原文）：正确记忆 / 遗漏 / 混淆 / 错误持久化 / 错误复用

**决策 4：全链路确定性（对应稳定性与可复现 15%）**

- 用例、数据生成种子、judge prompt 全部带版本号 pin 进 git
- 每次 run 产出 manifest（代码版本 + 配置哈希 + 环境信息 + judge 成本），支持两次 run 的结果 diff
- 同配置重复运行 N 次，报告方差与一致率；每个判定附证据引用（哪条对话、哪个记忆条目、哪个文件 diff）

### 1.4 六能力 × 用例族 × 验证方式矩阵

| 能力 | 用例族 | 注入设计 | 探测方式 | 五态判定要点 |
|---|---|---|---|---|
| 长期保持 persist | 稳定偏好/路径/模板 | 告知偏好 | 跨会话后问答 + 行为 | 记住=正确；忘=遗漏 |
| 记忆调用 recall | 任务中引用已知信息 | 告知后立即 + 延迟 | 问答 | 正确/遗漏 |
| 动态更新 update | 先 A 后改 B | 两轮矛盾信息 | 探测用哪个 | 用 B=正确；仍用 A=混淆 |
| 相近区分 discriminate | 相似对注入 | 两条相近信息 | 分别探测 | 交叉使用=混淆 |
| 边界识别 boundary | 敏感/临时/风险指令 | 临时口令、一次性路径 | 记忆 dump 检查 + 后续问答 | 不持久化=正确；持久化=错误持久化 |
| 任务复用 reuse | 跨会话重复任务 | 完成任务 1 | 下发相似任务 2 | 复用对=正确复用；复用错=错误复用 |

每族种子用例 ≥5 个、难度分 2 档，首轮总量 30–40；生成器保留 3 档难度与 60+ 扩量能力（4 周赛期压缩基线，见 §三；扩量叙事用于支撑"数据设计可扩展"评分点）。

### 1.5 用例 schema 骨架（B 的核心交付，示例）

```yaml
case_id: update-014
capability: dynamic_update
difficulty: 2
phases:
  - name: inject
    steps:
      - user: "我习惯把代码放在 ~/work/src"
      - user: "对了，改到 ~/dev/src 了，以后以这个为准"
  - name: confound
    steps:
      - user: "顺便聊聊别的……（无关话题 3 轮）"
    end_session: true
  - name: probe
    steps:
      - task: "帮我在我的代码目录下建个 demo 目录"
probes:
  - id: update-014-p1
    kind: rule                      # rule | judge
    assertion: "fs.path_exists ~/dev/src/demo"
    on_pass: correct_reuse
    on_fail:
      check: "fs.path_exists ~/work/src/demo"
      then: confusion
  - id: update-014-p2
    kind: judge
    ask: "我的代码目录在哪？"
    expect: "~/dev/src"
    rubric: "只答新路径为正确；答出旧路径判混淆"
```

### 1.6 指标体系（对应指标完整性 10%）

- **顶层**：六维雷达图（每能力 0–1 分），支持单智能体与多智能体叠加对比
- **每维内**：正确率 + 四类错误分解（遗漏/混淆/错误持久化/错误复用占比）
- **汇总**：可配权重总分；稳定性指标（重复运行一致率、judge 双判一致率）
- **报告**：markdown/HTML，每个判定点可下钻到证据原文

### 1.7 三大主打创新点（详见 design.md §11）

1. **测得真——系统级测试**（D+A）：真实重启 / 模拟隔天（改时钟）/ 系统回滚⚠️ / 记忆写入监控 / 多用户隔离 / 断网存活——每项都用 openKylin 3.0 原生机制，纯 API 评测做不到
2. **判得准——故障定位**（C+A）：对照"记忆库里存了什么"和"行为上用了什么"，把失败分成没存/存了没用上/存错了/该删没删；必要时从记忆库删一条再考一次做验证
3. **出题活——难度自适应**（C）：题目带难度参数（干扰/间隔/相似度三旋钮），实测校准区分度，按智能体水平挑题省 token，输出"从什么难度开始出错"的边界画像

辅助亮点：出题一致性检查（换说法/打乱顺序结果不应变）、随机假信息（字符串一搜就能判）、效率对比、MCP 统一操作记录⚠️、统一模型对照⚠️、Token 中心记账、**评测结果推 UKUI 桌面通知（长任务跑完弹雷达图）**。

---

## 二、五人分工

分工原则：**一人对准一个得分板块，主线互备、review 交叉**。A–D 四条技术主线由四位强手担纲，职责与原方案一致；新增 E 为交付与保障线——任务全部清单化、模板化，早期不占技术关键路径，W2 起跟学数据线成长为 B 的备份，中后期扛起交付收尾（打包试装 / 归档 / 视频 / 复现演练）。

### 角色 A：队长 · 架构与智能体适配（对准：任务定义与通用性 15% + 方案文档主线）

| Own（主责） | Backup（互备） |
|---|---|
| 总体架构、docs/contracts/ 接口契约 | C 的 schema 消费方 review |
| AgentAdapter 接口 + MockAdapter | D 的适配器测试环境 |
| KylinBot 适配器 → 第二个（OpenClaw）适配器 | |
| 方案说明文档总稿（交付 a 的方法论部分） | |
| 进度管理、集成、评审自评、答辩 | |

关键难点：智能体没有记忆导出接口时，直读其记忆文件/SQLite 或降级为纯行为探测——第一周就要把两个目标智能体的记忆存储位置摸清。

### 角色 B：数据工程（对准：数据设计质量 25%）

| Own | Backup |
|---|---|
| MemoryCase schema 定义与冻结 | D 的 runner 解析实现 |
| 六能力用例族设计（每族 ≥10、分 3 档难度） | C 的 probes 可判定性 review |
| 参数化用例生成器（模板 + LLM 辅助扩样 + 人工种子） | |
| 区分度校验：用例难度锚定（全对/全错的用例调难度） | |
| 交付 b 的样例数据部分 | |

关键难点：用例必须有**区分度**——评审明确关注"能否稳定区分不同智能体"。W4 起拿两个智能体的实测结果持续校准。

### 角色 C：评分算法（对准：自动评分 25% + 指标完整性 10%）

| Own | Backup |
|---|---|
| Evidence/Verdict schema 定义与冻结 | B 的用例 rubric 质量 review |
| 规则验证器全家（路径/正则/记忆库检索/文件 diff） | |
| LLM-judge v1：rubric + 锚定例 + 双判仲裁 + 五态判定 | |
| 指标计算、雷达图渲染、可解释报告生成 | |
| 交付 b 的样例结果部分 | |

关键难点：judge 稳定性。温度 0、prompt 版本化、双判一致率进报告；judge 成本记账。

### 角色 D：平台工程（对准：稳定性与可复现 15% + 落地 10% 技术侧 + 交付 c 技术内容）

| Own（主责） | Backup（互备） |
|---|---|
| Runner：三阶段编排、会话隔离、进程重启、文件系统快照 diff | A 的适配器联调 |
| 证据采集与存储（统一 JSONL + 哈希指纹 + manifest） | |
| CLI 工具（`memhall run/compare/report`） | |
| .deb 打包配置 + CI（打包执行与试装交给 E，按 checklist 走） | |
| openKylin 虚拟机/测试机环境与快照管理（E 协管预约表） | |
| 复现文档（README）技术内容 | |

关键难点：W1 就要把 openKylin 环境装好（最大隐藏风险，与 E 双人协同）；.deb 在 W2 末就打一版试装，不留到最后一周。

### 角色 E：交付与保障（对准：创新性与落地 10% 执行侧 + 交付 b/d/e 收尾 + 全队运转）

| Own（主责） | Backup（互备） |
|---|---|
| 交付 b 收口：样例任务/运行证据/评分结果的归档规范与执行 | B/C 供料 |
| .deb 打包执行 + 干净 openKylin 安装验证（照 D 的 checklist 逐项打勾） | D |
| 演示视频录制与剪辑（A 出脚本与叙事线） | |
| W4 交叉复现演练第一轮执行者，输出问题清单（"评审视角代言人"） | |
| 测试机预约表与快照纪律、共享网盘素材库（按交付 a–e 命名）、LLM 账号保管 | |
| docs/progress.md 与评审自评材料日常维护（A 审核） | |
| Hermes Agent 适配器（W2，照 A 的模板，直读 SQLite；A review，滑期 A 兜底） | A review |
| **（Backup）B 的参数化用例扩样**：W2 起跟学，W4 后独立顶班 | B review |

设计意图：E 的任务全部清单化、模板化，早期不占技术关键路径；W1–W3 跟学 case schema、生成器与 CLI 用法，中后期成长为交付收尾与复现验证的主力，同时补齐 B 的备份位。

### 评分点 → 责任人速查

| 评分维度 | 权重 | 主责 | 协同 |
|---|---|---|---|
| 任务定义与通用性 | 15% | A | 全员 |
| 数据设计质量 | 25% | B | C 出判定口径 |
| 自动评分能力 | 25% | C | B 出 rubric |
| 稳定性与可复现 | 15% | D | C 出一致率指标；E 出复现演练报告 |
| 指标完整性 | 10% | C | D 出图表载体 |
| 创新性与落地 | 10% | A 统稿 | D 出方案，E 执行落地 |

---

## 三、排期（4 周实际赛期，2026-09-23 重排）

> 赛事节点：报名截止 **10.11（周日）**；总决赛 **10.24（周六，上海）**；作品提交截止时间待官网确认。
>
> **压缩决策（原 8 周模板的明确砍单，全员周知，不做隐性扩量）**：
> ① 智能体改为**阶梯制四全用**（2026-09-23 决策，取代原"砍第三智能体"）：W2 硬承诺 KylinBot+Hermes，W3 增量 OpenClaw（过冒烟门才收），W4 可选 kylin-agent——任何增量不得推迟 M1–M4；② 用例首轮总量 60+ → 30–40（每族 ≥5），
> 生成器扩量能力保留作为"可扩展"叙事；③ 重复运行 3 次 → 2 次；④ .deb 试装从原 W5 提前到 W2 末；
> ⑤ 报名 W1 就办，不等 10.11 截止。

### W1 · 接口冻结 + 报名（09.23 三 – 09.27 日）

- 全员：吃透赛题与评分表（本文档已定稿）；**A 本周完成团队报名**并确认作品提交截止时间
- A：AgentAdapter 接口 + MockAdapter（能发消息、能吐假记忆 dump）+ 同步摸 KylinBot 记忆存储位置（原 W2 任务提前）
- B：MemoryCase schema v0.1 **冻结**（3 个种子用例）
- C：Evidence/Verdict schema v0.1 **冻结** + 规则验证器原型
- D：openKylin 虚拟机装好并做快照（基线见 environment.md：3.0 桌面版）；repo 初始化（monorepo + 分支保护 + CI 骨架）；Runner 骨架启动（原 W2 任务提前）
- E：随 D 装机并接管预约表/快照纪律；搭共享网盘素材库（按交付 a–e 命名）；通读赛题与本文档，W1 末能独立讲清全链路
- **M1（09.27 验收）**：MockAdapter + 3 用例 → runner→evidence→scoring→报告出图，假数据全链路冒烟（不可压）

### W2 · 并行开发 + 真实智能体接入（09.28 – 10.04）

- A：KylinBot 适配器完成（记忆存储无导出接口就直读文件/SQLite）→ 沉淀**三档接入体系**（CLI/API 通用协议模板零代码档 + 适配器基类参考实现）供 E 复用；W2 末启动 OpenClaw 调研
- B：六族用例每族 ≥5（总量 30+）+ 生成器 v1
- C：规则验证器全家 + judge v1（锚定/双判）+ 五态判定
- D：Runner 三阶段编排（重点：end_session 的会话隔离与进程重启）+ CLI 骨架 + **.deb 打包 v0.1** + **OS 体检套件 v0**（断电/时钟/离线三项，复用 runner 重启机制）+ **磐石/MCP/Token 中心可观测性摸底**（按 environment.md §7，决定套件最终形态）
- E：用 CLI 独立完成 MockAdapter 全流程操作（"第二个会跑全链路的人"）+ **Hermes Agent 适配器**（照 A 的模板做，直读其 SQLite 记忆，四个智能体里最友好；滑期不压里程碑，A 兜底）+ 按 checklist 执行 .deb 干净机试装
- **M2（10.04 验收）**：KylinBot × ≥10 用例端到端跑通，产出第一张真实雷达图；.deb v0.1 干净虚拟机可装

### W3 · 扩展与稳定（10.05 – 10.11，周日 = 报名截止）

- A：OpenClaw 适配完成（W3 增量：过冒烟门才收，不过则砍）→ 三智能体（KylinBot/Hermes/OpenClaw）对比打通；Hermes 若 W2 未收口则本周兜底；**doctor 自动探测命令**（复用写入监控定位记忆文件，零代码档的配套）
- B：用例扩到 30–40 上限 + 区分度初校（≥3 智能体实测调难度）
- C：稳定性工程：固定 seed、2 次重复方差、judge 双判一致率报告；报告可解释性（判定 → 证据原文下钻）；**判卷质检三件套**（E 人工标 30–50 条金标准 + 故意错答案诱饵测试，符合率/拒绝率目标 ≥95%）
- D：CLI 三命令完善（run/compare/report）+ manifest/token 成本记账 + .deb 修缺 + **体检套件全量执行**（含磐石回滚、多用户、写入时机）出体检报告卡
- E：交付 b 归档 v1（样例任务/运行证据/评分结果）
- **M3（10.11 验收）**：≥3 智能体（硬承诺 KylinBot+Hermes；含 OpenClaw 若过门）× 全量 × 2 次重复方差达标；.deb 一键安装可用；**快照回滚 → quick 集跑通（复现性预演）**

### W4 · 文档与交付冲刺（10.12 – 10.18）

- A 主笔方案文档成稿（交付 a；B/C/D 供料）+ 视频脚本与叙事线
- （可选档）kylin-agent 适配：仅当前三智能体全部稳定且 owner 有余力时启动，只跑单遍全量；不过冒烟门直接砍
- B/C/D：各写所属章节与模块 README；D：可复现材料定稿（交付 c）；C：报告终版打磨
- E：演示视频粗剪 → 定稿（3–5 min：.deb 安装 → 双智能体批量评测 → 雷达图对比）；交付 b 定稿收口
- **全员交叉复现演练**：E 先跑第一轮并输出问题清单，随后每人从零 clone → 安装 → 跑通，模拟评审视角
- **M4（10.18 验收）**：交付 a–e 全部齐备，冻结代码与用例版本

### 决赛周 · 提交与答辩（10.19 – 10.24）

- 按官网截止时间提交全部材料（截止时间以官网为准；若早于 10.19，W4 内并行提前提交）
- 模拟评审互相挑刺（拿评分表逐项过）、答辩材料、录屏补拍
- **10.24（周六）上海总决赛**

---

## 四、协作规约（具体到工具与频率）

### 4.1 仓库

```
memhall/
├── docs/            # 方案文档（A 总稿）+ contracts/（接口契约）
├── schema/          # case/evidence/verdict schema（B/C 各管其半）
├── cases/           # 种子用例库（B）
├── generators/      # 用例生成器（B）
├── adapters/        # 智能体适配器（A）
├── runner/          # 三阶段编排（D）
├── evidence/        # 证据采集与存储（D）
├── scoring/         # 评分引擎（C）
├── report/          # 指标与雷达图（C）
├── cli/             # CLI 入口（D）
├── packaging/       # debian/ 打包配置（D）
└── tests/           # 端到端测试
```

- 平台：Gitee 或 GitHub（国内访问优先 Gitee，双远端镜像推送）
- 分支：`main` 保护；`feat/<模块>-<内容>` 短命分支；PR 须 1 人 review 才可合并
- **跨模块 review 规则**：schema 类文件改动必须由消费方 owner 过审（case schema → C+D；adapter 接口 → D；evidence schema → C）
- commit 规范：`module(scope): what`，如 `scoring(judge): add dual-judge arbitration`
- 大文件不入 git：证据目录、录屏、VM 镜像 gitignore；归档走共享网盘，git 只留 manifest 与小样例

### 4.2 每周节奏

| 时点 | 事项 | 形式 |
|---|---|---|
| 周一 30min | 计划会：对齐本周目标、审批接口变更 | 会议 |
| 每日 | 三行异步站会：昨日完成/今日计划/阻塞 | 群消息 |
| 有阻塞当天 | 15min 临时拉会（不过夜） | 会议 |
| 周五 | 集成日：全部 PR 合 main、跑端到端冒烟，红灯周末前修或回滚 | CI + D 执行 |
| 周日晚 | E 更新 docs/progress.md（A 审核）：六评分点完成度 % + 风险清单 | 文档 |

### 4.3 接口纪律（并行开发不翻车的关键）

- W1 冻结的三个 schema（case / adapter / evidence）后续任何改动 = 开 issue + @受影响方 review + 附迁移说明
- 先在 `docs/contracts/` 写契约再写代码；契约与实现不一致以契约为准，改契约走流程

### 4.4 共享资源

- **openKylin 测试机**：预约表（在线表格：时间段/人/用途/快照状态，E 维护），用完恢复快照；虚拟机镜像放共享网盘
- **LLM judge API**：统一账号由 E 保管，key 放 `.env`（模板文件入库，真 key 不入 git）；设预算上限，每次 run 的 token 成本写入 manifest（C 记账）
- **素材库**：共享网盘目录按交付物 a–e 命名对齐，E 搭建与维护

### 4.5 文档责任矩阵

| 文档 | 主笔 | 供料 |
|---|---|---|
| 方案说明文档（交付 a） | A | §数据集设计→B，§自动评分→C，§复现与工具→D |
| 项目 README（交付 c） | D | 各模块 README 由 owner 自维护；E 以复现演练视角 review |
| 演示视频（交付 e） | E | A 出叙事线与脚本，全员出镜可选 |

每两周 A 拿评分表自评一轮（六项打分 + 证据链接），低于预期的项在周一计划会上升优先级。

### 4.6 风险登记

| 风险 | 概率 | 应对 | Owner |
|---|---|---|---|
| 智能体无记忆导出接口 | 高 | 直读记忆文件/SQLite；纯行为探测降级 | A |
| openKylin 环境踩坑 | 高 | W1 即装虚拟机（D+E 双人）；快照回滚 | D |
| LLM judge 漂移 | 中 | 温度 0 + 锚定 + 双判 + 规则仲裁 | C |
| 用例无区分度 | 中 | W3 起双智能体实测校准难度 | B |
| .deb/视频最后一周翻车 | 中 | W2 末先打一版 .deb（D 配置 + E 试装）、W4 出视频粗剪（E） | D+E |
| E 线早期产出不稳 | 中 | 任务清单化 + checkpoint 制；W1 只考核环境/管理件交付，技术活全部有强手 Backup | A |
| 赛期压缩范围失控 | 高 | 智能体四全用走阶梯门：增量过冒烟才收、不得推迟 M1–M4；其余隐性加量一律先砍后议 | A |
| 四智能体 token 成本超预算 | 中 | 预算按智能体分列（E 记账）；重复运行只做硬承诺组与过门智能体，kylin-agent 可选档只跑单遍 | E+C |
| 作品提交截止时间未知 | 中 | 报名后立即官网确认并回填排期基线行；按 10.18 收口倒排 | A |
| 进度不均/单点卡死 | 中 | Backup 制 + 阻塞不过夜 | A |

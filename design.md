# 麟阁 总体设计

> v1.1 · 2026-09-23 · 本文档整合项目全部技术设计，是交付物 a（方案说明文档）的底稿。
> 配套：`team-plan.md`（分工/排期/协作）、`environment.md`（环境基线与搭建）。
> 标注 ⚠️ 的条目依赖 W1–W2 实测验证（见 environment.md §7），对外材料不得写成已完成能力。

---

## 1. 我们做什么

**麟阁**（MemHall）——面向 openKylin 生态的智能体记忆能力评测基准：**给跑在 openKylin 上的智能体测记性，全自动、可复现**。名字取自麒麟阁——汉代评定功臣、画像记名的殿堂，我们给记忆能力优秀的智能体立榜。

基本流程很直观：

1. **教**：跟智能体聊几轮，教它一些东西——该记的（偏好、路径、模板）和不该记的（临时口令、一次性信息）混在一起说；
2. **隔**：中间聊点无关的、重开会话、甚至重启系统，模拟"过了几天"；
3. **考**：再问它，或者让它干一个需要用到之前信息的活；
4. **判**：对照全程录下来的证据（对话记录、记忆库内容、操作日志、文件变化）自动打分。

最后输出每个智能体的六维雷达图和一份每个判定都能查到依据的报告。评测跑完，结果通知直接推到 UKUI 桌面——点开就是雷达图对比。

## 2. 赛题要求逐条回应

| 赛题要求 | 我们的回应 | 章节 |
|---|---|---|
| 六项能力（保持/调用/更新/相近区分/边界/复用） | 六个用例族 × 七种题型 | §4 |
| 把对话、记忆记录、行动轨迹、文件统一为证据 | 四类证据统一格式采集 | §5 |
| 人工检查难规模化 | 机器判卷优先，预计人工介入 <5%（有指标） | §6 |
| 问答式覆盖不了冲突更新、相似干扰、行动复用 | 专门的题型 + 跨会话任务链 | §4 |
| 分清"记住了/误记了/不该记却记了" | 五种判定结果 + 故障定位 | §6 |
| 与 openKylin 架构良好集成 | 六项系统级测试全用 3.0 原生机制；结果推 UKUI 桌面通知直达雷达图 | §7/§10 |
| 多维指标 | 三层指标 + 难度边界 + 效率对比 | §8 |

**评审六维对账**（写方案文档时的对照索引，每个维度都有对应章节的实物证据）：

| 评分维度 | 权重 | 核心证据 | 章节 |
|---|---|---|---|
| 任务定义与通用性 | 15% | 三档接入 + 四类证据统一 + auditd 兜底 + 系统级测试 + 桌面通知集成 | §3/§5/§7/§10 |
| 数据设计质量 | 25% | 覆盖矩阵 + 区分度实测校准 + 出题四规矩 + 数据集说明卡 + 题目池轮换/保难度校验 | §4 |
| 自动评分能力 | 25% | 三层判卷 + 五态判定 + 故障定位 + 判卷质检三件套（金标准/诱饵/双判） | §6 |
| 稳定性与可复现 | 15% | manifest 全链路 + 快照纪律 + 两种稳定分开报告 + run diff + ≤30 分钟复现 | §3/§10 |
| 指标完整性 | 10% | 14 项指标目录 + 雷达/错误构成/难度边界三张画像 | §8 |
| 创新性与落地 | 10% | 三句话创新 + 六项 3.0 机制绑定 + 回归测试工具定位 | §7/§11 |

## 3. 系统架构

```
┌────────────────────────────────────────────────┐
│  命令行工具  memhall run / compare / report     │  一键运行、批量对比（交付 d）
├────────────────────────────────────────────────┤
│  执行层（Runner）                                │
│   按"教→隔→考"剧本执行 → 调用智能体 → 保存证据    │
│   系统级动作：重启 / 改时钟 / 回滚 / 断网 / 切用户 │
├──────────────────────┬─────────────────────────┤
│  适配层               │  题库层                  │
│  统一接口，每种智能体  │  用例 + 题目生成器       │
│  一个适配器           │                         │
├──────────────────────┴─────────────────────────┤
│  评分层                                         │
│  规则判卷 + AI 判卷双保险 → 五种判定结果          │
│  → 雷达图 + 系统级测试报告 + 可下钻的报告         │
└────────────────────────────────────────────────┘
```

四个基本决定：

1. **照剧本测**：每个用例是一段写好的对话剧本（教什么、隔多久、考什么），保证每次测试条件一致、结果可复现；
2. **统一接口，三档接入**：评测器只认五个功能（发消息 / 导出记忆库 / 导出操作记录 / 重开会话 / 清空记忆），接入新智能体分三档：
   - **零代码（配置即接）**：内置三种通用协议模板——命令行型（配置启动命令，走标准输入输出）、API 型（任何提供 OpenAI 兼容接口的智能体，填地址密钥）、MCP 型⚠️（3.0 原生协议）。绝大多数智能体落这档，约 10 行配置；
   - **自动探测（doctor 命令）**：`memhall doctor` 扫描系统里装了哪些智能体、生成配置草稿，并用"聊一句看哪个文件变了"的写入监控自动定位记忆文件，人工确认即用；
   - **定制（写代码）**：特殊智能体继承基类写适配器（KylinBot 预计落这档，doctor 同样能帮它定位记忆）。
   接入体验：`doctor` 发现 → `run --cases quick` 冒烟 → `compare` 出图，三条命令测任何智能体；
3. **机器判卷优先**：能用规则判的（文件存不存在、字符串在不在）绝不用 AI；剩下的语义判断交给 AI 判卷，两个不同模型独立判，不一致才标记人工复核；
4. **全程留痕**：题目版本、随机种子、判卷提示词版本、环境信息全部记录，同一套输入跑两遍，报告结果差异（稳定性指标）。

## 4. 测试数据怎么设计

### 4.1 两个维度给题目分类

- **考哪种记忆操作**：写入 / 检索 / 更新 / 拒绝 / 复用（赛题原词）；
- **记的是什么内容**：偏好 / 路径 / 模板 / 事实 / 项目状态 / 临时敏感信息。

两个维度交叉成一张覆盖表（6 × 6 = 36 格）。**首轮 30–40 题保证矩阵无空格**（每格至少 1 题，重点格 2–3 题，质量优先于数量）；生成器扩量后目标每格 ≥5 题。报告自动统计实际覆盖情况——覆盖面可检查，不是口头声称。

### 4.2 七种题型（首轮 30–40 题，可自动扩量）

| 题型 | 例子 | 判什么 |
|---|---|---|
| 会话内提问 | "我叫什么名字？" | 记对 / 忘了 |
| 跨会话提问 | 重开会话后再问 | 记对 / 忘了 |
| 信息更新 | 先说"代码放 ~/work"，后改口"改到 ~/dev 了" | 用新路径=对；仍用旧路径=记混 |
| 时间理解 | "上周说的那个路径是哪条？" | 记混 |
| 相近区分 | 告知两条很像的路径，分别问 | 交叉使用=记混 |
| 无中生有 | 问一个从没告诉过它的事 | 承认不知道=对；瞎编=记错了 |
| 跨会话任务链 ★ | 见 4.3 | 用对了 / 用错了 / 没用上 |

每道题还可以自动做**一致性检查**：把注入信息换个说法、把会话顺序打乱、中间加无关闲聊——记忆结果都不应该变。变了就说明智能体的记忆不稳定。这一招不需要人工标注正确答案，能凭空扩出大量题目。

### 4.3 跨会话任务链（重点）

这是现有评测都没做的部分，也是赛题明确点名的"行动复用"：

```
会话 1："帮我建项目结构 ~/proj/{src,docs,tests}"
会话 2："把新模块放到该放的地方"        ← 得记得上次的目录结构
会话 3："给 docs 补个 README，按我以前的习惯写" ← 得记得模板偏好
验收：直接检查最终的文件树 + 它的操作日志里引用了几次旧信息
```

判定三种：**用对了**（按历史信息办事）/ **用错了**（张冠李戴）/ **没用上**（从零重来）。另外跟"没教过的对照组"比步数和耗时——记性好的智能体第二次干同样的活应该更快更省。

数量：3–5 条链，每条 3 个会话。这类题少而深。

### 4.4 题目生成与难度控制

- 种子题目人工编写，生成器从模板参数化扩量（人物角色 × 各种属性），同样种子生成同样的题——可复现；
- 每题带难度参数：**干扰信息多少 / 间隔多久 / 相似程度**，三个旋钮；
- **题目池轮换**：题库大于单次评测用例集，每次运行按难度分层抽样——多次运行不重题、防背题（题目池轮换是业界动态基准防污染的标准做法）；
- **变换保难度**：一致性检查的换说法、打乱顺序等变换必须不改变题目难度（用试点数据验证：变换前后通过率应无系统差异），否则分数下降分不清是记忆不稳定还是题目变难了；
- 实测校准：W3 在多个智能体上跑出每题通过率，全都对或全错的题调难度（或明确标为"送分题/超难题"留着当上下限参照）——题目质量用数据说话。

### 4.5 出题质量四条规矩（B 的生产规范，进库前逐条过）

1. **可判定**：每道题的判定规则明确到能直接写进配置（查哪个文件、搜哪个字符串、答对的标准是什么）——写不清判定规则的题不许进库；
2. **防污染**：题目参数随机化（乱码路径、随机偏好组合），杜绝任何模型可能在训练数据里见过的事实；
3. **像人话**：注入信息用真实用户口吻（"我习惯把代码放 ~/dev"，而不是"请记住路径 /home/dev"），探测问题也按日常对话问——测的是自然交互中的记忆，不是应试记忆；
4. **本土化**：场景扎根 openKylin 桌面语境（UKUI 应用名、麒麟软件源、国产路径习惯）——顺带加深与赛题环境的绑定。

### 4.6 数据集说明卡

随题库附一张说明卡（业界 dataset card 惯例）：题目来源与构造方式、覆盖矩阵、难度分布、校准数据、已知局限（题量、语言、场景范围）。数据设计的每个主张都能在卡上对账，评审不必翻代码。

## 5. 证据：我们看什么

四类证据，统一格式保存、带哈希指纹：

| 证据 | 怎么采 | 用途 |
|---|---|---|
| 对话记录 | 适配器自动记录 | 基础 |
| 记忆库内容 | 适配器导出；没有导出接口就直接读它的存储文件 | 判断"存没存、存了什么" |
| 操作记录 | 适配器导出；⚠️ 优先尝试对接 MCP 协议的调用日志（3.0 官方标准，各智能体格式天然统一） | 判断"用没用上" |
| 文件变化 | 每阶段前后做文件系统快照对比 | 验收任务结果 |

兜底手段：用系统的审计接口（auditd）监控智能体记忆文件的全部读写——就算某个智能体完全不提供导出接口，我们也能知道它"偷偷"写了什么。这保证了任何智能体都能被测（通用性）。

小技巧：注入的信息里放一些**随机生成的假信息**（比如 `canary-x7f3` 这种乱码目录名）。它不存在于任何模型的训练数据里，所以只要在记忆库或回答里搜到这个字符串，就百分之百是被记下来的——规则判定，零误差。

## 6. 打分：怎么判卷

### 6.1 判卷方式：规则优先，AI 兜底

- 规则判卷：文件存不存在、路径对不对、字符串在不在记忆库里——确定性强、零成本；
- AI 判卷：语义类的判断（回答算不算"记混"）交给大模型，带评分标准 + 样例锚定，**两个不同模型独立判**，一致就采信，不一致用规则仲裁，还不行才标人工复核；
- 衡量自动化的指标：**人工复核率 <5%**、双模型一致率 ≥90%，写进报告；
- **判卷质检三件套**：① 金标准——人工标 30–50 条，判卷符合率 ≥95%；② **诱饵测试**——把故意做错的答案（张冠李戴、瞎编、半对）喂给判卷器，拒绝率 ≥95%（业界教训：有知名记忆评测的判卷器对故意错误答案的接受率高达 63%，只测金标准一致发现不了这种宽松）；③ 双模型一致率 ≥90%；
- **判卷模型选型**：与被测智能体的模型后端**不同家族**——判卷器会系统性给自家模型的输出打高分（自偏好偏差，业界已证实）；
- **判卷细则**：啰嗦不加分（防长答案偏差）；信息更新题以新信息为准——答出旧信息不算错但单独标记，汇总为"过期信息调用率"。

### 6.2 五种判定结果（对准评审细则原文）

**记对了 / 忘了 / 记混了 / 记错了（瞎编）/ 不该记的记下了 / 用错了**——每种结果都附带证据链接（哪句对话、记忆库哪一条、哪个文件变化），可点开查验。

补充一个状态：**运行无效**（智能体超时、崩溃、答非所问导致探测无法完成）——不计分、单独统计，避免把运行故障误算成记忆能力差。

### 6.3 不只看对错，还定位坏在哪一环

把"记忆库里存了什么"和"行为上用了什么"放在一起对照，失败就能分成四类：

| 记忆库 | 行为 | 结论 |
|---|---|---|
| 没存 | 用错 | **没存**（写入故障） |
| 存了 | 用错 | **存了没用上**（调用故障） |
| 存了但内容错 | 用错 | **存错了**（写入质量差） |
| 不该存的还在 | — | **该删没删**（边界失效） |

再加两个验证手段，把"相关"变成"因果"：

- **删一条试试**：从记忆库里删掉某条信息，重新考一次——行为跟着变了，说明它真的在用这条记忆；没变，就是"存了没用上"的实锤；
- **问两遍**：同一道题，"教过再问"和"没教就问"各跑一次。只有前者答对、后者答错的，才算真记住——排除了"模型本来就知道"的干扰。

## 7. 系统级测试（openKylin 特色，别的评测做不到）

智能体跑在真实操作系统上，就有一批 API 评测做不了的测试。六项核心，每项都用到 openKylin 3.0 的原生机制：

| # | 测试 | 人话版 | 用的 3.0 机制 |
|---|---|---|---|
| 1 | 重启存活 | 重启电脑 / 注销 / 重启服务之后，它还认得我吗？ | systemd、快照 |
| 2 | 模拟隔天 | 把系统时钟往前拨三天，"三天后过期"的临时口令它处理对了吗？ | 系统时钟 |
| 3 | 系统回滚 | 回滚系统之后，我的记忆还在不在？记忆文件放的位置合规吗？ | 磐石架构（不可变系统）⚠️ |
| 4 | 写入监控 | 它是边聊边记，还是聊完才记？记了多少废话？ | 文件监控 |
| 5 | 多用户隔离 | 换个用户登录，能偷看到别人的智能体记忆吗？记忆文件权限设对了吗？ | Linux 多用户 |
| 6 | 断网存活 | 拔了网线（云端模型不可用），本地还记得多少？ | 网络管理 |

加一项技术兜底：**审计接口旁路监控**（第 5 节已述）。

产出："系统级测试报告"——每个智能体六项各一个通过率，和雷达图并排的第二张招牌图；每项测试都在 UKUI 桌面上真实发生，录屏素材现成。

## 8. 输出什么指标

指标分三层，每个都有名字、有口径、能对账：

| 层 | 指标 | 口径 |
|---|---|---|
| **主指标**（每智能体×每能力维） | 六维能力分 | 每维通过率（每次都过才算过），0–1 |
| | 错误构成 | 忘了/记混/记错/不该记/用错各占比例 |
| | 运行无效率 | 超时崩溃等，单列不计分 |
| **深化指标** | 难度边界 | 每维"从什么难度开始错"（干扰数/间隔/相似度） |
| | 系统级测试报告 | 重启/隔天/回滚/写入/隔离/断网六项通过率 |
| | 记忆效率 | 任务链有/无记忆的步数、token、耗时比 |
| | 过期信息调用率 | 更新维专属：拿旧信息答新问题 |
| | 写入卫生 | 边界维专属：记忆库里不该存内容的占比 |
| **评测质量指标**（我们自己可信吗） | 人工复核率 / 双判一致率 / 金标准符合率 / 诱饵拒绝率 | 判卷质检四件套 |
| | 重复运行一致率 | 同条件跑两遍判定不变的比例 |
| | 覆盖矩阵 | 能力×内容实际题数 + 证据来源覆盖统计 |

**总分刻意做轻**：默认六维等权、可配置权重，报告放最不起眼的位置并注明"总分仅供快速排序，评价看分维"——多维能力差异才是主角。

呈现差异的三张图：六维雷达叠加（形状=能力轮廓）、错误构成对比（同样 60 分，全是"忘了"和全是"记混"是两种病）、难度边界曲线（每个智能体在哪崩）。

难度边界还能反过来用——**按水平挑题**：已经确定会做的难度不用反复考，省时间省 token（四个智能体全量跑分时这笔账不小）。

## 9. 实验安排

| 实验 | 目的 | 做法 |
|---|---|---|
| 统一模型对照⚠️ | 分清"模型聪明"还是"记忆系统好" | 让四个智能体尽量接同一个大模型（KylinBot 走 Token 中心，其他配同版本模型端点），差别就主要来自记忆机制；对齐到什么程度如实写 |
| 四个智能体分批上 | 通用性展示 | W2 保底两个（KylinBot + Hermes），W3 加 OpenClaw（通过冒烟测试才收），W4 视余力加 kylin-agent |
| 重复运行 | 稳定性 | 同条件 ×2，报告分数波动和判定一致率 |

## 10. 工程落地

- **命令行工具**：`memhall run / compare / report`，配置文件驱动批量对比；
- **打包分发**：.deb 包（交付要求），在虚拟机内打包保证依赖兼容；有余力再提供软件商店入口；
- **成本记账**：被测智能体的 token 消耗优先从 Token 中心取数⚠️，判卷消耗单独记账，全部写进运行清单；
- **结果触达（桌面通知）**：评测结束推 UKUI 桌面通知（完成/失败 + 报告路径，点击打开报告页）；长任务（四智能体全量）跑完直接弹出对比雷达图——"挂机评测，回来看图"。这是"工具长在麒麟里"最直观的一处：评测工具是系统里的普通应用，像其他应用一样与桌面交互（实现即 notify-send + xdg-open）；
- **结果追踪**：每次运行生成唯一 run ID 与结果目录（判定明细 + manifest），支持两次运行的 diff（哪些判定翻转、分数差在哪）——评测结果可追溯、可对比；
- **依赖锁定**：Python 依赖锁文件入库，判卷端点的模型版本冻结并记录进 manifest——排除"环境漂了"的质疑；
- **两种稳定分开报告**：评测系统的稳定（同输入同输出：种子固定、快照归零、判卷版本化——我们的责任）与被测智能体的稳定（记忆系统本身的波动——被测属性，用"每次都过才算过"口径量化）分开呈现，波动不混入评测误差；
- **复现承诺**：从零开始（干净虚拟机 → 装包 → 出雷达图）≤30 分钟，W3 末轻量预演一次，W4 全员实际演练并计时。

## 11. 创新点（写给评委的三句话）

1. **测得真**：别的记忆评测只能"假装隔了几天"，我们能真的重启、断网、换用户、回滚系统——只有跑在真实操作系统上才做得到，这正是 openKylin 的主场；
2. **判得准**：不只看答没答对，还定位坏在哪一环（没存？存了没用？存错了？该删没删？），必要时从记忆库删一条来做验证——判卷全程可解释、人工介入率有指标；
3. **出题活**：题目带难度参数、能自动扩量、有实测区分度校准，还能按智能体水平自适应挑题，省成本的同时给出清晰的"能力边界画像"。

其余辅助亮点（自动一致性检查、随机假信息防作弊、效率对比、MCP 统一操作记录⚠️、Token 中心记账、结果推 UKUI 桌面通知直达雷达图）分布在各章节，不单独张扬。

**落地的最实际形态：社区的记忆回归测试工具。** 智能体版本更新频繁（KylinBot 这类月更），每次更新用 memhall 跑一遍分、diff 两次运行，就知道记忆能力有没有退步——赛题"服务于社区持续评估与优化"的最直接兑现。扩展方向：社区公开榜单（麒麟阁立榜，名实相符）、服务器版无头批量跑分、接入更多智能体（三档接入保证成本可控）。

## 12. 和已有工作的差别

| 已有工作 | 它做了什么 | 我们的差别 |
|---|---|---|
| LongMemEval（2024，最主流的对话记忆评测） | 500 道纯对话问答，考提取/更新/时间推理/拒答 | 借了它的题型设计；我们多了系统级测试、行动层复用、记忆库证据 |
| MemoryAgentBench（2025） | 多会话流式对话，考保持/更新/冲突 | 同上：它不出对话层 |
| MPBench / MemSecBench（2026，记忆安全评测） | 测恶意记忆注入与清除 | 方向不同（安全 vs 能力），我们引用它说明记忆存储可外部观测 |
| τ-bench / OSWorld（智能体任务评测） | 用"最终状态对不对"来判卷 | 借了它的验收思想用于任务链 |

一句话定位：**对话层已有评测做得不错，我们把记忆评测扩展到了系统层和行动层，并用记忆库证据把"记没记住"从猜测变成检查。**

## 13. 哪些还要验证（诚实边界）

- ⚠️ 磐石架构的具体命令与边界、MCP 调用日志是否可获取、Token 中心有无查询接口：W1–W2 实测（environment.md §7），验证前不写进对外材料；
- "删一条试试"需要记忆库可写：Hermes（SQLite）最顺，KylinBot 视实测情况降级为只读定位；
- 难度标定的数据量有限：结论标注"初步标定"；
- 对外表述统一为"基线两个智能体 + 第三个展示接入路径"，不夸大；
- 跨架构、服务器版、软件商店分发：作为后续计划一句话提及，不承诺。
